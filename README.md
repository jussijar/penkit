# penkit

## Compile & Run

```bash
  # Start mongo daemon in another shell
  $ mongod run

  $ npm install
  $ lein compile

  # Start app
  $ node target/main.js

  # Open http://localhost:1337/
```